# Roadmap

路线图记录可以独立验收的工作，不代表兼容性承诺。

## 0.3

- 提供 HTTP 客户端适配示例，验证超时和状态码到 `AttemptFailure` 的映射。
- 增加等待队列超时与取消原因。
- 增加滑动窗口限流器，并与现有两类算法使用相同决策接口。
- 为配置字段增加版本标记和废弃提示。

## 0.4

- 提供异步任务执行适配层。
- 支持指标事件订阅接口，减少中间数组分配。
- 增加状态序列化格式，用于进程重启后的恢复实验。
- 验证 JavaScript、Wasm 和 Native 后端的一致行为。

## 发布前

- 固定公开 API 命名并补齐文档测试。
- 对 4xx、5xx、超时和取消等常见错误分类给出建议。
- 建立性能基准，记录策略链每次执行的分配和耗时。
- 增加版本升级与迁移说明。
